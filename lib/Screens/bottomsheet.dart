import 'package:flutter/material.dart';

class BottomSheetForPastData extends StatelessWidget {
  const BottomSheetForPastData({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: TextButton(child: Text('Past data'),
        onPressed: (){
          //
        },
        ),
      ),
    );
  }
}