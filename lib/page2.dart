import 'package:flutter/material.dart';

class Page2 extends StatelessWidget {
  // Implement the UI for your Page2 here
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page 2'),
        backgroundColor: Colors.indigoAccent[200], // Same color as home page app bar
      ),
      body: Center(
        // Add your desired content for Page2
      ),
    );
  }
}
