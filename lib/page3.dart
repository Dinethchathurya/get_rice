import 'package:flutter/material.dart';

class Page3 extends StatelessWidget {
  // Implement the UI for your Page3 here
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page 3'),
        backgroundColor: Colors.indigoAccent[200], // Same color as home page app bar
      ),
      body: Center(
        // Add your desired content for Page3
      ),
    );
  }
}
